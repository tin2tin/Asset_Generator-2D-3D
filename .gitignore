# Installer-generated at runtime (pip-installed packages, cloned repos,
# compiled extensions) — regenerated by install_all_dependencies(), never
# committed. wheels/*.whl (the small prebuilt extension wheels) IS committed.
addon_packages/
TRELLIS_REPO/
TRELLIS_EXT/
PIXAL3D_REPO/
__pycache__/
install_log.txt
