# Frozen dependency set for the 2D Asset Generator & Trellis 3D add-on.
#
# This is the FULL closure of runtime packages (top-level + transitive), pinned
# to known-good versions. The installer applies it with `pip install --no-deps`
# so NO package is allowed to resolve/pull its own dependencies — every needed
# package is already listed here at an exact version. This is what prevents a
# dependency from dragging in a second, CPU-only torch/numpy and shadowing the
# CUDA build (which is what broke `torch.cuda.is_available()`).
#
# NOT listed here (installed by dedicated steps in __init__.py):
#   * torch / torchvision / torchaudio  -> installed from the CUDA (+cu128) index
#   * o-voxel, flexgemm, cumesh, nvdiffrast, trellis2  -> compiled from source
#
# Package names use the installed (normalized) form; pip treats '_' / '-' / '.'
# as equivalent, so e.g. `opencv_python` resolves to `opencv-python` on PyPI.

accelerate==1.14.0
annotated_doc==0.0.4
anyio==4.14.1
attrs==26.1.0
certifi==2026.6.17
charset_normalizer==3.4.7
click==8.4.2
colorama==0.4.6
# diffusers dev build — must come from git (not published on PyPI)
git+https://github.com/huggingface/diffusers.git
easydict==1.13
einops==0.8.2
filelock==3.29.4
flatbuffers==25.12.19
fsspec==2026.6.0
glcontext==3.0.0
h11==0.16.0
hf_xet==1.5.1
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.21.0
idna==3.18
imageio==2.37.3
imageio_ffmpeg==0.6.0
importlib_metadata==9.0.0
jinja2==3.1.6
jsonschema==4.26.0
jsonschema_specifications==2025.9.1
kornia==0.8.3
kornia_rs==0.1.14
lazy_loader==0.5
llvmlite==0.47.0
markdown_it_py==4.2.0
markupsafe==3.0.3
mdurl==0.1.2
moderngl==5.12.0
mpmath==1.3.0
networkx==3.6.1
ninja==1.13.0
numba==0.65.1
numpy==2.5.0
onnxruntime_directml==1.24.4 ; sys_platform == "win32"
onnxruntime_gpu==1.27.0 ; sys_platform != "win32"
opencv_python==4.13.0.92
packaging==26.2
pillow==12.2.0
platformdirs==4.10.0
plyfile==1.1.4
pooch==1.9.0
protobuf==7.35.1
psutil==7.2.2
pygments==2.20.0
pymatting==1.1.15
pyyaml==6.0.3
referencing==0.37.0
regex==2026.6.28
rembg==2.0.76
requests==2.34.2
rich==15.0.0
rpds_py==2026.6.3
safetensors==0.8.0
scikit_image==0.26.0
scipy==1.18.0
setuptools==82.0.1
shellingham==1.5.4
sympy==1.14.0
tifffile==2026.6.1
timm==1.0.27
tokenizers==0.22.2
tqdm==4.68.3
transformers==5.12.1
trimesh==4.12.2
triton_windows==3.7.1.post27 ; sys_platform == "win32"
typer==0.25.1
typing_extensions==4.15.0
urllib3==2.7.0
# utils3d pinned to a specific commit — must come from git
git+https://github.com/EasternJournalist/utils3d.git@9a4eb15e4021b67b12c460c7057d642626897ec8
wheel==0.47.0
zipp==4.1.0
zstandard==0.25.0
